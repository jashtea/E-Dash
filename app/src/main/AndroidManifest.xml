<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EDash"
        tools:targetApi="31">

        <!-- Start app in Home -->
        <activity
            android:name=".Home"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Stocks and related activities -->
        <activity
            android:name=".Stocks"
            android:exported="true" />
        <activity
            android:name=".Stock_in"
            android:exported="false" />
        <activity
            android:name=".StockOutActivity"
            android:exported="false" />

        <!-- Other existing activities -->
        <activity android:name=".Owners_Income" android:exported="false" />
        <activity android:name=".Login" android:exported="true" />
        <activity android:name=".Income" android:exported="true" />
        <activity android:name=".Top_Products" android:exported="false" />
        <activity android:name=".MandW" android:exported="false" />
        <activity android:name=".Editprofile" android:exported="false" />
        <activity android:name=".MainActivity" android:exported="false" />
        <activity android:name=".Monitor_Sales" android:exported="false" />
        <activity android:name=".Profile" android:exported="false" />

    </application>

</manifest>
